import * as React from 'react';
import { StatelessComponent } from 'react';

export const AboutPage: StatelessComponent = () => {
    return (
        <div>
            <h1>About</h1>
            <p>MERN + TypeScript Starter</p>
        </div>
    );
}